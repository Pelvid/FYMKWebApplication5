@model IEnumerable<FYMKWebApplication5.Models.Verify>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/_LayoutPage1.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Csharp)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Python)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CSS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Csharp)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Python)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CSS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
