CREATE TABLE MatchCheckbox (
    OrderID int NOT NULL,
    OrderNumber int NOT NULL,
    PersonID int,
    PRIMARY KEY (MentessID),
    FOREIGN KEY (PersonID) REFERENCES Persons(PersonID)
);

select Id from Mentees as MenteeId

select Id from Mentors as The
select Id from CheckB
select CheckBox, Id from CheckB

drop table ProfileSettings

CREATE TABLE CheckB (
    id              INT           NOT NULL    IDENTITY    PRIMARY KEY,
    Checkbox BIT,
      
);

select * from CheckBox